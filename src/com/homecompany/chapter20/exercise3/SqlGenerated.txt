creation SQL is :
CREATE TABLE TableOfPerson (
    FIRSTNAME VARCHAR(30),
    LASTNAME VARCHAR(50),
    HANDLE VARCHAR(30) PRIMARY KEY,
    HEIGHT FLOAT,
    WEIGHT FLOAT,
    PERSONID BIGINT NOT NULL UNIQUE,
    AGE INT);
